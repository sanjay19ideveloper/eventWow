import '../../../core/app_export.dart';/// This class is used in the [rewindlist_item_widget] screen.
class RewindlistItemModel {RewindlistItemModel({this.rewind, this.id, }) { rewind = rewind  ?? ImageConstant.imgRewind;id = id  ?? ""; }

String? rewind;

String? id;

 }
