import '../../../core/app_export.dart';/// This class is used in the [verification_item_widget] screen.
class VerificationItemModel {VerificationItemModel({this.one, this.id, }) { one = one  ?? "1";id = id  ?? ""; }

String? one;

String? id;

 }
